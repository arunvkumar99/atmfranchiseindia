-- Remove the get_started_submissions table and all related data
-- This table is no longer needed as we're redirecting to become-franchise page instead

DROP TABLE IF EXISTS public.get_started_submissions CASCADE;