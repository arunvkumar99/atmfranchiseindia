// Translation Status Component - Disabled (requires database)
import React from 'react';

const TranslationStatus: React.FC = () => {
  // Component disabled - no database connection available
  // Previously used Supabase for translation status tracking
  return null;
};

export default TranslationStatus;